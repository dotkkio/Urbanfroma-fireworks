package com.urbanforma.fireworks.content.saturn;

/** Stable mapping to the three RGB channels supplied by the owning firework style. */
public enum SaturnColorBand {
    PRIMARY,
    SECONDARY,
    ACCENT
}
